apiVersion: v1
kind: Service
metadata:
  name: forsys-agent
  namespace: biodiversity
  labels:
    app: forsys-agent
spec:
  type: ClusterIP
  selector:
    app: forsys-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
